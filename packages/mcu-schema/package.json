{
  "name": "@mcuhelper/mcu-schema",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "extract-cards": "npm run build && node scripts/extract-userguide-cards.mjs",
    "test": "npm run build && node --test dist/**/*.test.js"
  }
}
