export declare function expandIncludes(text: string, baseDir: string): {
    text: string;
    includes: string[];
    errors: string[];
};
export declare function expandRepeats(text: string): string;
